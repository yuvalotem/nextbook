import PersonIcon from '@mui/icons-material/Person';
import { styled } from '@mui/material';
import { iconStyle } from './style';

export const AccountIcon = styled(PersonIcon)(({ theme }) => iconStyle(theme))
