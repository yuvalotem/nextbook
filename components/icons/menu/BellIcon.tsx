import NotificationsIcon from '@mui/icons-material/Notifications';
import { styled } from '@mui/material';
import { iconStyle } from './style';

export const BellIcon = styled(NotificationsIcon)(({ theme }) => iconStyle(theme))
