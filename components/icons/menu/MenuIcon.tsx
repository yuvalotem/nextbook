import MoreVertIcon from '@mui/icons-material/MoreVert';
import { styled } from '@mui/material';
import { iconStyle } from './style';

export const MenuIcon = styled(MoreVertIcon)(({ theme }) => iconStyle(theme))