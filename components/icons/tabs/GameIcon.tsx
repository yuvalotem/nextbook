import ExtensionIcon from '@mui/icons-material/Extension';
import { styled } from '@mui/material';
import { iconStyle } from './style';

export const GameIcon = styled(ExtensionIcon)(({ theme }) => iconStyle(theme))