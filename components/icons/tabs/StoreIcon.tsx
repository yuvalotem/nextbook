import StorefrontIcon from '@mui/icons-material/Storefront';
import { styled } from '@mui/material';
import { iconStyle } from './style';

export const StoreIcon = styled(StorefrontIcon)(({ theme }) => iconStyle(theme))